# TitleFormatter

> **Original titles must be preserved!** Keep in mind changing scene titles on personal preferences disqualify the scenes for contributing to StashDB.

Automatically cleans, formats and manipulates scene titles. Can remove technical tags, performer names, normalizes quotes/dashes, replace patterns and applies configurable casing (e.g. title case). All rules are opt-in.
